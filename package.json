{
  "name": "obs-studio-node",
  "version": "1.0.0",
  "description": "Experimental bindings to obs-studio using nan directly",
  "main": "./distribute/index.js",
  "types": "./distribute/index.d.ts",
  "scripts": {
    "clean": "rimraf *.{js,map} distribute/*.{js,map} obs-build build",
    "compile": "tsc && tsc -p scripts",
    "install": "npm run build:obs && npm run build:bindings",
    "build:obs": "node build-obs.js",
    "build:bindings": "node build-bindings.js",
    "build:docs": "typedoc --out docs"
  },
  "author": "Zachary Lund <zachary.lund@streamlabs.com>",
  "license": "GPL-2.0",
  "devDependencies": {
    "rimraf": "^2.6.1",
    "typescript": "^2.4.2",
    "typedoc": "^0.8.0",
    "typedoc-default-themes": "^0.5.0"
  },
  "dependencies": {
    "@types/shelljs": "^0.7.2",
    "cmake-js": "^3.5.0",
    "unzipper": "^0.8.9",
    "nan": "^2.6.2",
    "shelljs": "^0.7.8",
    "simple-git": "^1.74.1"
  }
}