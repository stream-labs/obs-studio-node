
mkdir ${SLBUILDDIRECTORY}

cd ${SLBUILDDIRECTORY}

echo $PWD
echo ${SLFULLDISTRIBUTEPATH}
echo ${RUNTIMENAME}
echo ${RUNTIMEURL}
echo ${ELECTRONVERSION}
echo ${RELEASENAME}
echo ${BUILDCONFIG}

cmake .. \
-DCMAKE_INSTALL_PREFIX=$PWD/../${SLFULLDISTRIBUTEPATH}/obs-studio-node \
-DSTREAMLABS_BUILD=OFF \
-DNODEJS_NAME=${RUNTIMENAME} \
-DNODEJS_URL=${RUNTIMEURL} \
-DNODEJS_VERSION=${ELECTRONVERSION} \
-DLIBOBS_BUILD_TYPE=${RELEASENAME} \
-DCMAKE_BUILD_TYPE=${BUILDCONFIG}

cd ..